let defaultToken = localStorage.token || "";
let defaultUserName = localStorage.user || "";

export default {
  token: defaultToken,
  userName: defaultUserName,
  menuStatus: false,
  navPopupShow: false
};
